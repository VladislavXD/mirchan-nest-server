// Prisma 7 config placeholder. Not used in Prisma 5.x.
// Оставлено пустым, чтобы не ломать сборку TypeScript.
//import "dotenv/config";
//import { defineConfig, env } from "prisma/config";
//
//export default defineConfig({
//  schema: "prisma/schema.prisma",
//  migrations: {
//    path: "prisma/migrations",
//  },
//  datasource: {  // Используем прямой DATABASE_URL (в .env добавлен полностью развёрнутый URL)
//    url: env("DATABASE_URL"),
//  },
//});
export {}
